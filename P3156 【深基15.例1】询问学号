#include <iostream>
#include <vector>
#include <algorithm>
int main() {
	using namespace std;
	int n, m;
	cin >> n >> m;
	vector<int> Num(n + 1);
	for (int i = 1; i <= n; i++) cin >> Num[i];
	for (int i = 1; i <= m; i++) {
		int temp;
		cin >> temp;
		cout << Num[temp] << endl;
	}
	return 0;
}
