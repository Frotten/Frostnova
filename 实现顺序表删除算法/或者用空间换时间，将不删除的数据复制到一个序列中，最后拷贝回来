#include <stdio.h>
int main()
{
    int n;
    scanf("%d",&n);
    int arr[1005];
    for(int i=1;i<=n;i++){
        scanf("%d",&arr[i]);
    }
    for(int i = 1;i<=n;i++){
        if(i%3==0)continue;
        printf("%d ",arr[i]);
    }
    return 0;
}
